import 'package:flutter/material.dart';
import 'package:experimental/splashscreen/splash_screen.dart';
// Change import to correct file name

void main() {
  // Use the correct class name (with const)
  runApp(SplashScreen());
}
